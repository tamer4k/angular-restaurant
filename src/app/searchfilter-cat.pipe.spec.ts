import { SearchfilterCatPipe } from './searchfilter-cat.pipe';

describe('SearchfilterCatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterCatPipe();
    expect(pipe).toBeTruthy();
  });
});
